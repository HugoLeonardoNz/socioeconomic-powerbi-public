__pycache__/
*.pyc
*.pyo
.env
.venv
venv/
.ipynb_checkpoints/
*.egg-info/
dist/
build/
.DS_Store

# backup que o build cria antes de reescrever o .pbix
*.prebuild.pbix
# Backups datados do modelo, feitos antes de reconstruir a camada visual.
# Ficam locais: o .pbix versionado ja e a fonte, e binario duplicado no
# historico pesa sem informar.
*.bak.pbix
